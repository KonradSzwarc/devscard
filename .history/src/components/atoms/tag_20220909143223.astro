---
export interface Props extends astroHTML.JSX.HTMLAttributes {}

const { props } = Astro;
---

<tag
    type="span"
    {...props}
    class:list={[
        'text-black text-center text-base text-sm font-medium align bg-gray-200',
        props.class,
    ]}
    ><slot />
</tag>
