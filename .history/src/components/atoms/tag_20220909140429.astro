---
export interface Props extends astroHTML.JSX.HTMLAttributes {}

const { props } = Astro;
---

<tag
    type="span"
    {...props}
    class:list={[
        'text-black bg-gray-200',
        props.class,
    ]}
    ><slot />
</tag>
